cat > render.yaml <<'EOF'
services:
  - type: web
    name: source-impact-mlm
    env: docker
    plan: free
    region: oregon
    dockerfilePath: docker/app/Dockerfile
    autoDeploy: true
    healthCheckPath: /healthz.html
    envVars:
      - key: PORT
        value: "8080"
      - key: APP_ENV
        value: production
EOF